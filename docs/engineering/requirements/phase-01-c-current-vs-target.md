# AayurFace — Phase 01-C Requirements Engineering Audit
## Document: Current State vs. Target State Reconciliation Matrix

**Phase:** Phase 01-C — Requirements Evidence Reconciliation, Claim Correction & Quality Hardening  
**Date:** 2026-09-03  
**Status:** RECONCILED & AUDITED  
**Core Invariant:** Requirements define the TARGET state. Audits document the CURRENT implementation gaps. Never confuse the two.  

---

### 1. Architectural Reconciliation Matrix

| Domain | Current Repository State (Level 1/2/3 Evidence) | Target Engineering Requirement (Level 5/6 Source) | Empirical Evidence / File Reference | Architectural Gap | Current Status |
|---|---|---|---|---|---|
| **1. Authentication** | Mock `localStorage` implementation storing fake sessions; passwords completely ignored (`src/contexts/AuthContext.tsx`). | Cryptographic Supabase Auth with bcrypt password hashing, rotating JWTs, and secure sign-out (`FR-AUTH-001..005`). | `src/contexts/AuthContext.tsx:52-96`; `package.json` has `@supabase/supabase-js`. | Critical security vulnerability; no server-side identity verification. | **IMPLEMENTATION PENDING** |
| **2. Authorization** | Zero role verification; Edge Function accepts `userId` directly from client request body without checking JWT. | Strict server-side `auth.uid() = user_id` enforcement on all queries and Edge Functions (`NFR-SEC-003`). | `supabase/functions/analyze-skin/index.ts:43`; client-side route guards mock. | Insecure Direct Object Reference (IDOR) risk across all routes. | **IMPLEMENTATION PENDING** |
| **3. User Profile** | Basic profile fields stored in localStorage; `profiles` SQL table exists with minimal schema. | Rich demographic, skin concern, and wellness goal profile in PostgreSQL (`FR-AUTH-007`). | `supabase/schema.sql:11-20`; `src/pages/app/ProfilePage.tsx`. | Database schema lacks extended fields (age range, skin concerns, language). | **IMPLEMENTATION PENDING** |
| **4. Consent Lifecycle** | Zero consent tracking in database or UI; onboarding bypasses consent entirely. | Granular unbundled informed consent (biometrics, storage, research) with immutable audit records (`FR-CONSENT-001..005`). | No consent component in `src/pages/onboarding/`; no `consents` table in SQL schema. | Complete absence of biometric consent collection mechanism. | **IMPLEMENTATION PENDING** |
| **5. Questionnaire** | 3 hardcoded skin concerns in UI; zero constitutional questions or dosha vector calculation. | 15-question validated constitutional intake mapping to normalized V/P/K vector (`FR-AYU-001..006`). | `src/pages/onboarding/OnboardingPage.tsx:14-25`; no `questionnaire_responses` table. | Missing core Ayurvedic phenotypic intake engine and database schema. | **IMPLEMENTATION PENDING** |
| **6. Lifestyle Context** | Non-existent; no environmental, sleep, diet, or stress intake forms. | Structured lifestyle form capturing diet, sleep, stress, climate, hydration with downstream logic (`FR-LIFE-001..004`). | No lifestyle components in `src/pages/`; no `lifestyle_contexts` table. | Missing 1 of the 3 multimodal fusion input channels. | **IMPLEMENTATION PENDING** |
| **7. Camera Capture** | Basic `react-webcam` component with a 3-second `setTimeout` simulating capture (`src/pages/app/ScanPage.tsx`). | Standardized video stream capture with real-time UI guides and encrypted frame upload (`FR-CAP-001..009`). | `src/pages/app/ScanPage.tsx:17-22`. | Capture is entirely simulated; no real capture pipeline. | **IMPLEMENTATION PENDING** |
| **8. Quality Gateway** | Non-existent; capture button triggers timer regardless of lighting, distance, or blur. | Client-side 468-point Face Mesh evaluating lighting, blur, centering, distance, and single face (`CV-CAP-001..008`). | No MediaPipe or TensorFlow packages in `package.json`; no quality gating logic. | Zero quality gating; system accepts black frames, blurred frames, multiple faces. | **IMPLEMENTATION PENDING** |
| **9. Computer Vision (CV)** | Uncalled generic GPT-4o Vision API wrapper without local feature extraction (`analyze-skin`). | Server-side non-diagnostic feature extraction (redness index, texture roughness, melanin uniformity) (`FR-CV-001..006`). | `supabase/functions/analyze-skin/index.ts:70-110`. | Lack of dedicated computer vision algorithms; relies on unvalidated LLM vision prompt. | **IMPLEMENTATION PENDING** |
| **10. Ayurvedic Engine** | Static keyword substring matching on 3 terms (`acne`, `dry`, `pitta`) in `ChatPage.tsx`. | Curated Ayurvedic reasoning mapping visual and constitutional signals to classical balancing principles (`FR-AYU-002`). | `src/pages/app/ChatPage.tsx:56-80`. | Intelligence is entirely simulated via 3 static `if/else` checks. | **IMPLEMENTATION PENDING** |
| **11. Multimodal Fusion** | Non-existent; demo results hardcoded in `mockData.ts`. | Configurable weighted combination of visual, questionnaire, and lifestyle vectors (`FR-FUS-001..004`). | `src/data/mockData.ts:50-98`; no fusion scripts or services in repository. | Single-modality simulation disguised as multimodal analysis. | **IMPLEMENTATION PENDING** |
| **12. Confidence & Conflict** | Static fake percentage badge (e.g., "94% Confidence") in mock data. | Deterministic confidence formula based on quality, completeness, and cosine agreement (`FR-CONF-001..004`). | `src/pages/app/ResultsPage.tsx:28-40` renders static strings from mock data. | System cannot detect conflicting evidence; creates false confidence. | **IMPLEMENTATION PENDING** |
| **13. Explainable AI (XAI)** | Generic static text block ("Your skin shows signs of Pitta imbalance..."). | 5-part structured XAI contract (observations, context, agreement, meaning, boundary) (`FR-XAI-001..003`). | `src/data/mockData.ts:55-65`. | No causal trace; explanations are hardcoded strings. | **IMPLEMENTATION PENDING** |
| **14. RAG Knowledge Base** | Non-existent; remedies loaded from static array `MOCK_REMEDIES`. | `pgvector` knowledge repository with classical text chunking, `text-embedding-3-small`, and citations (`FR-RAG-001..005`). | `supabase/schema.sql` lacks `pgvector` extension and embeddings table. | High hallucination risk; remedies ungrounded in classical texts. | **IMPLEMENTATION PENDING** |
| **15. Personalization** | Static recommendations based on hardcoded demo scan IDs. | Prioritized recommendations with recipe instructions and mandatory 24h patch test notice (`FR-REC-001..005`). | `src/pages/app/ResultsPage.tsx:85-110`. | Recommendations lack standardized safety contracts and citations. | **IMPLEMENTATION PENDING** |
| **16. Voice Assistant** | Non-existent in UI; floating mic icon does not exist or trigger speech APIs. | Web Speech API integration for SpeechRecognition and SpeechSynthesis in user's language (`FR-VOICE-001..003`). | No Web Speech API references in `src/`. | Voice interaction completely missing from codebase. | **DEFERRED TO V2** |
| **17. Localization (i18n)** | English-only hardcoded strings throughout UI; no i18n framework. | Pluggable i18next runtime supporting English and Hindi (MVP) with regional font rendering (`FR-I18N-001..005`). | `package.json` lacks `i18next` or `react-i18next`; all JSX contains raw English. | Total absence of multilingual support. | **IMPLEMENTATION PENDING** |
| **18. History View** | Renders static list from `MOCK_SCANS`; database `scan_results` never queried. | Dynamic history timeline queried from `scan_results` with RLS isolation and agreement filters (`FR-HIST-001..002`). | `src/pages/app/HistoryPage.tsx:18-35` maps over mock array. | Historical records are disconnected from persistent database storage. | **IMPLEMENTATION PENDING** |
| **19. Progress Tracking** | Static SVG mockup in `ProgressPage.tsx`; no dynamic chart engine. | Longitudinal trend analytics (Recharts) for visual signals and adherence over 30/60/90 days (`FR-PROG-001..003`). | `package.json` lacks `recharts`; `src/pages/app/ProgressPage.tsx` uses static SVGs. | Returning users cannot track real longitudinal skin changes. | **DEFERRED TO V2** |
| **20. PDF Report Export** | Non-existent; clicking report download has no handler. | Authenticated PDF wellness report generation with full insight summary and disclaimers (`FR-PDF-001..002`). | No PDF generation package (`@react-pdf/renderer` or `jspdf`) in `package.json`. | Users cannot export or print their analysis results. | **DEFERRED TO V1** |
| **21. Research Platform** | Non-existent; no research panels, annotator queues, or dataset exports. | Triple-practitioner consensus annotation portal and Fitzpatrick bias evaluation (`FR-ADMIN-008`). | No research routes or tables. | Advanced clinical research capabilities completely unbuilt. | **RESEARCH / R&D SCOPE** |
| **22. Security Controls** | Insecure: mock auth, unauthenticated edge functions, wildcard CORS, IDOR exposure. | Hardened: Supabase bcrypt auth, verified JWT claims, strict CORS, RLS on 100% tables, signed URLs (`NFR-SEC-001..008`). | Phase 00 Audit Doc 05 (`05-security-assessment.md`). | Comprehensive security refactoring required across frontend and backend. | **IMPLEMENTATION PENDING** |
| **23. Automated Testing** | Exactly 1 test file (`Logo.test.tsx`, 2 unit tests passing in 83ms). | Automated test suites covering Auth, Quality Gateway, Fusion math, RLS policies, and E2E flows (`NFR-MAINT-003`). | `vitest` in `package.json`; 1 test in `src/components/common/Logo.test.tsx`. | Near-zero automated test coverage across all business logic and security paths. | **IMPLEMENTATION PENDING** |
| **24. Observability** | Zero logging, telemetry, latency tracking, or error reporting. | Structured JSON logging with `x-correlation-id`, capture failure telemetry, and inference monitoring (`NFR-OBS-001..004`). | No logging libraries or Sentry/telemetry hooks in `src/` or `supabase/`. | Blind production operations; zero diagnostic visibility. | **IMPLEMENTATION PENDING** |
| **25. CI/CD & Build Health** | `npm run build` fails due to TS2769 in `vite.config.ts`; `npx vite build` passes; no GitHub Actions. | Clean `tsc -b && vite build` execution; automated CI workflow validating lint, type-check, and tests. | Terminal execution in Phase 00; `.github/workflows` does not exist. | Broken standard build command; absence of automated deployment pipeline. | **IMPLEMENTATION PENDING** |
